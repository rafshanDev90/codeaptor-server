version: '3.8'

services:
  app:
    build: .
    container_name: codeaptor-app
    restart: always
    environment:
      - PORT=3000
      - NODE_ENV=production
      - MONGO_URI=${MONGO_URI}
      - CLERK_PUBLISHABLE_KEY=${CLERK_PUBLISHABLE_KEY}
      - CLERK_SECRET_KEY=${CLERK_SECRET_KEY}
      - CLIENT_ORIGIN=${CLIENT_ORIGIN}
      - CLERK_WEBHOOK_SECRET=${CLERK_WEBHOOK_SECRET}
    ports:
      - "3000:3000"
